# PA-04 — Virtual camera attacks

Feed a pre-recorded clip through a **virtual camera** (e.g. OBS Virtual Camera).

Document software name and version in manifest `notes`.

Optional for standard tier; recommended before production.
